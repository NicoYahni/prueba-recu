import {StyleSheet} from 'react-native';

const styleText = StyleSheet.create({
      nombre: {
        color:'red'
      }
    })
  //podes ir agregando mas, cuando los agregas los tenes que poner en el export de aca y en el import de la App.js



export {styleText}